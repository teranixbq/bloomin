refactor: replace 'corpus' concept with 'knowledge'

- Rename corpus_url to knowledge throughout the codebase
- Change from URL-based (Google Drive) to direct Telegram input
- Add automatic migration from corpus_url to knowledge on startup
- Update setup flow to ask for knowledge text instead of URL
- Add backward compatibility for legacy {corpus} placeholder in system prompts
- Update bot commands: /reload now reloads knowledge from config

Breaking change: Users must re-enter knowledge via /setup or it will be 
automatically migrated from existing corpus_url on first boot.